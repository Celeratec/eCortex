# Git Secrets Pattern File
# Used with git-secrets to prevent committing sensitive data
# Install: brew install git-secrets (macOS) or see https://github.com/awslabs/git-secrets

# AWS Keys
[A-Z0-9]{20}
AKIA[0-9A-Z]{16}

# Generic API Keys and Tokens
[aA][pP][iI][-_]?[kK][eE][yY].*[=:]["']?[A-Za-z0-9_\-]{20,}
[aA][pP][iI][-_]?[sS][eE][cC][rR][eE][tT].*[=:]["']?[A-Za-z0-9_\-]{20,}
[tT][oO][kK][eE][nN].*[=:]["']?[A-Za-z0-9_\-]{20,}

# Passwords in config
[pP][aA][sS][sS][wW][oO][rR][dD].*[=:]["']?[^"'\s]{8,}
[pP][aA][sS][sS][wW][dD].*[=:]["']?[^"'\s]{8,}

# MongoDB connection strings with credentials
mongodb://[^:]+:[^@]+@

# Session keys
[sS][eE][sS][sS][iI][oO][nN][-_]?[kK][eE][yY].*[=:]["']?[A-Za-z0-9_\-]{20,}

# Private keys
-----BEGIN (RSA |EC |DSA |OPENSSH )?PRIVATE KEY-----
-----BEGIN ENCRYPTED PRIVATE KEY-----

# MeshCentral specific
meshinstall=[A-Za-z0-9]{10,}
InstallToken.*[=:]["']?[A-Za-z0-9]{10,}

# Allowed patterns (false positives)
# Add patterns here that should be ignored
